using System;

namespace NumbersToWords
{
    class Program
    {
        static void Main(string[] args)
        {
            {
                
                {
                    string[] words = { "one", "two", "three", "four", "five", "six", "seven", "eight", "nine", "ten" };
                    string[] manywords = { "eleven", "twelve", "thirteen", "fourteen", "firfteen", "sixteen", "seventeen", "eighteen", "nineteen" };
                    string[] manywords2 = { "twenty", "thirthy", "forthy", "fifty", "sixty", "seventy", "eighty", "ninty" };
                    {
                        for (int counter = 0; counter < 70; counter++)
                            // int Coverter = counter-- > words;
                            foreach (string word in manywords2)
                            {
                                                                
                                {
                                        string s = Convert.ToString(counter.ToString);
                                

                        //  foreach (string word in manywords2)
                            {
                            Console.WriteLine("");
                            }
                        }
                    }
                }

            }
        }
    }
}
}

